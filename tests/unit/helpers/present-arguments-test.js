import { presentArguments } from '../../../helpers/present-arguments';
import { module, test } from 'qunit';

module('Unit | Helper | present arguments');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = presentArguments(42);
  assert.ok(result);
});
